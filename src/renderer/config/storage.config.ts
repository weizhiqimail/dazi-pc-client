/** 集中登记渲染层使用的全部持久化键名。 */
export const STORAGE_KEYS = {
  cacheDirectory: 'dazi.cacheDirectory',
  theme: 'dazi.theme',
  density: 'dazi.density',
  contentWidth: 'dazi.contentWidth',
  navFontScale: 'dazi.navFontScale',
  contentFontScale: 'dazi.contentFontScale',
} as const;
