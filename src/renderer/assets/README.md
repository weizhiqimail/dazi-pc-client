# Renderer assets

Place renderer-owned images, icons, fonts, and other static resources in this directory. Prefer SVG for interface artwork and keep large runtime data or downloaded models outside the application bundle.
